# PrivateBin — environment variable reference
# Copy relevant lines to a .env file alongside docker-compose.yaml
# Most configuration is done via /srv/cfg/conf.php (mounted config volume)

# Timezone
TZ=America/New_York

# Hostname
BASE_HOST_NAME=${HOSTNAME}
